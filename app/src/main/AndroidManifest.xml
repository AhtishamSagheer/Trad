<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ahtisham.sagheer.trading2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Authantication.TermAndConditionActivity"></activity>
        <activity android:name=".Authantication.PervicyPolicyActivity" />
        <activity android:name=".ListingActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".PayBillActivity" />
        <activity android:name=".Authantication.LoginActivity" />
        <activity android:name=".Authantication.RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>